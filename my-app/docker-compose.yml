version: "3.9"

services:
  app:
    image: ghcr.io/OWNER/REPO/app:latest
    container_name: ci-cd-sample-app
    ports:
      - "80:3000"
    restart: always
    environment:
      - NODE_ENV=production
